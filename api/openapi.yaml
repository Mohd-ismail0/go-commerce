openapi: 3.0.3
info:
  title: Rewrite Commerce API
  version: 0.4.0
servers:
  - url: http://localhost:8080
security:
  - ApiTokenAuth: []
components:
  securitySchemes:
    ApiTokenAuth:
      type: http
      scheme: bearer
      bearerFormat: API token
      description: Static API token from API_AUTH_TOKEN environment variable.
    RoleHeaderAuth:
      type: apiKey
      in: header
      name: X-Role
      description: Role header baseline for sensitive routes.
    UserHeaderAuth:
      type: apiKey
      in: header
      name: X-User-JWT
      description: Signed user JWT (HS256) used for identity and role/permission checks.
  parameters:
    TenantHeader:
      in: header
      name: X-Tenant-ID
      required: true
      schema:
        type: string
    RegionHeader:
      in: header
      name: X-Region-ID
      required: true
      schema:
        type: string
    IdempotencyHeader:
      in: header
      name: Idempotency-Key
      required: true
      schema:
        type: string
        minLength: 8
        maxLength: 128
    UserIdHeader:
      in: header
      name: X-User-JWT
      required: false
      schema:
        type: string
    RegionIdQuery:
      in: query
      name: region_id
      required: false
      schema:
        type: string
  schemas:
    ErrorResponse:
      type: object
      required: [code, message]
      properties:
        code:
          type: string
        message:
          type: string
        details:
          type: object
          additionalProperties: true
    PaginationMeta:
      type: object
      required: [next_cursor]
      properties:
        next_cursor:
          type: string
    ProductListResponse:
      type: object
      required: [items, pagination]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/Product"
        pagination:
          $ref: "#/components/schemas/PaginationMeta"
    Product:
      type: object
      required: [id, tenant_id, region_id, sku, name, currency, price_cents]
      properties:
        id:
          type: string
          minLength: 3
        tenant_id:
          type: string
        region_id:
          type: string
        sku:
          type: string
          minLength: 1
          maxLength: 64
        name:
          type: string
          minLength: 1
          maxLength: 256
        currency:
          type: string
          pattern: "^[A-Z]{3}$"
        price_cents:
          type: integer
          format: int64
          minimum: 1
    ProductInput:
      type: object
      required: [sku, name, currency, price_cents]
      properties:
        id:
          type: string
        sku:
          type: string
          minLength: 1
          maxLength: 64
        name:
          type: string
          minLength: 1
          maxLength: 256
        currency:
          type: string
          pattern: "^[A-Z]{3}$"
        price_cents:
          type: integer
          format: int64
          minimum: 1
    ProductVariant:
      type: object
      required: [id, tenant_id, region_id, product_id, sku, name, currency, price_cents]
      properties:
        id: { type: string }
        tenant_id: { type: string }
        region_id: { type: string }
        product_id: { type: string }
        sku: { type: string }
        name: { type: string }
        currency:
          type: string
          pattern: "^[A-Z]{3}$"
        price_cents:
          type: integer
          format: int64
          minimum: 1
    ProductVariantInput:
      type: object
      required: [sku, name, currency, price_cents]
      properties:
        id: { type: string }
        sku: { type: string }
        name: { type: string }
        currency:
          type: string
          pattern: "^[A-Z]{3}$"
        price_cents:
          type: integer
          format: int64
          minimum: 1
    Category:
      type: object
      required: [id, tenant_id, region_id, name, slug]
      properties:
        id: { type: string }
        tenant_id: { type: string }
        region_id: { type: string }
        name: { type: string }
        slug: { type: string }
        parent_id: { type: string }
    CategoryInput:
      type: object
      required: [name, slug]
      properties:
        id: { type: string }
        name: { type: string }
        slug: { type: string }
        parent_id: { type: string }
    Collection:
      type: object
      required: [id, tenant_id, region_id, name, slug]
      properties:
        id: { type: string }
        tenant_id: { type: string }
        region_id: { type: string }
        name: { type: string }
        slug: { type: string }
    CollectionInput:
      type: object
      required: [name, slug]
      properties:
        id: { type: string }
        name: { type: string }
        slug: { type: string }
    ProductMedia:
      type: object
      required: [id, tenant_id, region_id, product_id, url, media_type]
      properties:
        id: { type: string }
        tenant_id: { type: string }
        region_id: { type: string }
        product_id: { type: string }
        url: { type: string }
        media_type: { type: string }
    ProductMediaInput:
      type: object
      required: [url, media_type]
      properties:
        id: { type: string }
        url: { type: string }
        media_type: { type: string }
    ItemsResponse:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items: {}
    OrderListResponse:
      type: object
      required: [items, pagination]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/Order"
        pagination:
          $ref: "#/components/schemas/PaginationMeta"
    Order:
      type: object
      required: [id, tenant_id, region_id, customer_id, status, total_cents, currency]
      properties:
        id:
          type: string
        tenant_id:
          type: string
        region_id:
          type: string
        customer_id:
          type: string
        status:
          type: string
          enum: [created, confirmed, completed, cancelled]
        total_cents:
          type: integer
          format: int64
          minimum: 1
        currency:
          type: string
          pattern: "^[A-Z]{3}$"
        updated_at:
          type: string
          format: date-time
    OrderCreateInput:
      type: object
      required: [customer_id, total_cents, currency]
      properties:
        id:
          type: string
        customer_id:
          type: string
        status:
          type: string
          enum: [created]
        total_cents:
          type: integer
          format: int64
          minimum: 1
        currency:
          type: string
          pattern: "^[A-Z]{3}$"
        voucher_code:
          type: string
        promotion_id:
          type: string
        tax_class_id:
          type: string
        country_code:
          type: string
    OrderStatusUpdate:
      type: object
      required: [id, status, expected_updated_at]
      properties:
        id:
          type: string
        status:
          type: string
          enum: [created, confirmed, completed, cancelled]
        expected_updated_at:
          type: string
          format: date-time
    Customer:
      type: object
      properties:
        id: { type: string }
        tenant_id: { type: string }
        region_id: { type: string }
        email: { type: string }
        name: { type: string }
    PriceBookEntry:
      type: object
      properties:
        id: { type: string }
        tenant_id: { type: string }
        region_id: { type: string }
        product_id: { type: string }
        currency: { type: string }
        amount_cents: { type: integer, format: int64 }
    StockItem:
      type: object
      properties:
        id: { type: string }
        tenant_id: { type: string }
        region_id: { type: string }
        product_id: { type: string }
        quantity: { type: integer, format: int64 }
    TaxClass:
      type: object
      properties:
        id: { type: string }
        tenant_id: { type: string }
        region_id: { type: string }
        name: { type: string }
    TaxRate:
      type: object
      properties:
        id: { type: string }
        tenant_id: { type: string }
        region_id: { type: string }
        tax_class_id: { type: string }
        country_code: { type: string }
        rate_basis_points: { type: integer, format: int64 }
    Voucher:
      type: object
      properties:
        id: { type: string }
        tenant_id: { type: string }
        region_id: { type: string }
        code: { type: string }
        discount_type: { type: string }
        value_cents: { type: integer, format: int64 }
        currency: { type: string }
        usage_limit: { type: integer, format: int64, nullable: true }
        used_count: { type: integer, format: int64 }
        starts_at: { type: string, format: date-time }
        ends_at: { type: string, format: date-time }
    PromotionRule:
      type: object
      properties:
        id: { type: string }
        tenant_id: { type: string }
        region_id: { type: string }
        promotion_id: { type: string }
        rule_type: { type: string }
        value_cents: { type: integer, format: int64 }
        currency: { type: string }
    PricingCalculationInput:
      type: object
      required: [currency, base_amount_cents]
      properties:
        currency: { type: string }
        base_amount_cents: { type: integer, format: int64 }
        voucher_code: { type: string }
        promotion_id: { type: string }
        tax_class_id: { type: string }
        country_code: { type: string }
    PricingCalculationResult:
      type: object
      properties:
        base_amount_cents: { type: integer, format: int64 }
        discount_cents: { type: integer, format: int64 }
        tax_cents: { type: integer, format: int64 }
        total_cents: { type: integer, format: int64 }
paths:
  /products:
    get:
      summary: List products
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
        - in: query
          name: cursor
          schema:
            type: string
        - in: query
          name: sku
          schema:
            type: string
      responses:
        "200":
          description: Product list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProductListResponse"
        "400":
          description: Invalid tenant/region
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    post:
      summary: Create or update product
      description: Idempotent upsert by product ID; retries with same payload are safe.
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
        - $ref: "#/components/parameters/IdempotencyHeader"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProductInput"
      responses:
        "201":
          description: Product created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Product"
        "400":
          description: Validation failure
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /orders:
    get:
      summary: List orders
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
        - in: query
          name: cursor
          schema:
            type: string
      responses:
        "200":
          description: Order list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrderListResponse"
    post:
      summary: Create order
      description: Idempotent create by `Idempotency-Key` per tenant.
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
        - $ref: "#/components/parameters/IdempotencyHeader"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OrderCreateInput"
      responses:
        "201":
          description: Order created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Order"
    patch:
      summary: Update order status
      description: Allowed transitions: created->confirmed/cancelled, confirmed->completed/cancelled.
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OrderStatusUpdate"
      responses:
        "200":
          description: Order updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Order"
        "400":
          description: Invalid status transition
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: Optimistic concurrency conflict
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /products/{product_id}/variants:
    get:
      summary: List product variants
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
        - in: path
          name: product_id
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Product variants
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ItemsResponse"
                  - type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: "#/components/schemas/ProductVariant"
    post:
      summary: Create product variant
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
        - in: path
          name: product_id
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProductVariantInput"
      responses:
        "201":
          description: Variant created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProductVariant"
    patch:
      summary: Update product variant
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
        - in: path
          name: product_id
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProductVariantInput"
      responses:
        "201":
          description: Variant updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProductVariant"
  /products/{product_id}/media:
    get:
      summary: List product media
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
        - in: path
          name: product_id
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Product media
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ItemsResponse"
                  - type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: "#/components/schemas/ProductMedia"
    post:
      summary: Create product media
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
        - in: path
          name: product_id
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProductMediaInput"
      responses:
        "201":
          description: Product media created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProductMedia"
  /categories:
    get:
      summary: List categories
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "200":
          description: Category list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ItemsResponse"
                  - type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: "#/components/schemas/Category"
    post:
      summary: Create category
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CategoryInput"
      responses:
        "201":
          description: Category created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Category"
  /collections:
    get:
      summary: List collections
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "200":
          description: Collection list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ItemsResponse"
                  - type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: "#/components/schemas/Collection"
    post:
      summary: Create collection
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CollectionInput"
      responses:
        "201":
          description: Collection created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Collection"
  /collections/{collection_id}/products/{product_id}:
    post:
      summary: Assign product to collection
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
        - in: path
          name: collection_id
          required: true
          schema: { type: string }
        - in: path
          name: product_id
          required: true
          schema: { type: string }
      responses:
        "201":
          description: Product assigned to collection
  /customers:
    get:
      summary: List customers
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "200":
          description: Customer list
    post:
      summary: Create or update customer
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "201":
          description: Customer saved
  /pricing:
    get:
      summary: List pricing entries
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "200":
          description: Price entries
    post:
      summary: Create or update pricing entry
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "201":
          description: Pricing entry saved
  /pricing/tax-classes:
    get:
      summary: List tax classes
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "200":
          description: Tax classes list
    post:
      summary: Create or update tax class
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "201":
          description: Tax class saved
  /pricing/tax-rates:
    get:
      summary: List tax rates
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "200":
          description: Tax rates list
    post:
      summary: Create or update tax rate
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "201":
          description: Tax rate saved
  /pricing/calculate:
    post:
      summary: Calculate deterministic price with discount and tax
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PricingCalculationInput"
      responses:
        "200":
          description: Calculation result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PricingCalculationResult"
  /inventory:
    get:
      summary: List inventory
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "200":
          description: Inventory list
    post:
      summary: Create or update inventory item
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "201":
          description: Inventory item saved
  /promotions:
    get:
      summary: List promotions
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "200":
          description: Promotions list
    post:
      summary: Create or update promotion
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "201":
          description: Promotion saved
  /promotions/rules:
    get:
      summary: List promotion rules
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "200":
          description: Promotion rules list
    post:
      summary: Create or update promotion rule
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "201":
          description: Promotion rule saved
  /promotions/vouchers:
    get:
      summary: List vouchers
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "200":
          description: Vouchers list
    post:
      summary: Create or update voucher
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "201":
          description: Voucher saved
  /regions:
    get:
      summary: List regions
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "200":
          description: Regions list
    post:
      summary: Create or update region
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "201":
          description: Region saved
  /brands:
    get:
      summary: List brands
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "200":
          description: Brand list
    post:
      summary: Create or update brand
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "201":
          description: Brand saved
  /payments:
    get:
      summary: List payments
      security:
        - ApiTokenAuth: []
          RoleHeaderAuth: []
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "200":
          description: Payment list
    post:
      summary: Create or update payment
      security:
        - ApiTokenAuth: []
          RoleHeaderAuth: []
          UserHeaderAuth: []
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
        - $ref: "#/components/parameters/IdempotencyHeader"
      responses:
        "201":
          description: Payment saved
  /payments/reconcile:
    get:
      summary: Reconcile payment ledger totals against payment status
      security:
        - ApiTokenAuth: []
          UserHeaderAuth: []
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "200":
          description: Reconciliation report
  /payments/{id}:
    get:
      summary: Get payment by id
      security:
        - ApiTokenAuth: []
          RoleHeaderAuth: []
          UserHeaderAuth: []
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Payment
  /payments/{id}/transactions:
    get:
      summary: List payment transactions
      security:
        - ApiTokenAuth: []
          RoleHeaderAuth: []
          UserHeaderAuth: []
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Transactions
  /payments/{id}/capture:
    post:
      summary: Capture authorized funds
      security:
        - ApiTokenAuth: []
          RoleHeaderAuth: []
          UserHeaderAuth: []
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
        - $ref: "#/components/parameters/IdempotencyHeader"
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Capture result
  /payments/{id}/refund:
    post:
      summary: Refund captured funds
      security:
        - ApiTokenAuth: []
          RoleHeaderAuth: []
          UserHeaderAuth: []
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
        - $ref: "#/components/parameters/IdempotencyHeader"
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Refund result
  /payments/{id}/void:
    post:
      summary: Void authorization before capture
      security:
        - ApiTokenAuth: []
          RoleHeaderAuth: []
          UserHeaderAuth: []
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
        - $ref: "#/components/parameters/IdempotencyHeader"
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Void result
  /webhooks/payments/{tenant_id}/{provider}:
    post:
      summary: Provider payment webhook (HMAC-signed when WEBHOOK_PAYMENT_SECRET is set)
      parameters:
        - name: tenant_id
          in: path
          required: true
          schema:
            type: string
        - name: provider
          in: path
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/RegionIdQuery"
      responses:
        "200":
          description: Webhook processed
  /shipping/zones:
    get:
      summary: List shipping zones
      security:
        - ApiTokenAuth: []
          RoleHeaderAuth: []
          UserHeaderAuth: []
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "200":
          description: Zones list
    post:
      summary: Create or update shipping zone
      security:
        - ApiTokenAuth: []
          RoleHeaderAuth: []
          UserHeaderAuth: []
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "201":
          description: Zone saved
  /shipping/resolve:
    post:
      summary: Resolve eligible shipping methods for country, channel, postal prefix, and order total
      security:
        - ApiTokenAuth: []
          RoleHeaderAuth: []
          UserHeaderAuth: []
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "200":
          description: Eligible methods
  /shipping/methods:
    get:
      summary: List shipping methods
      security:
        - ApiTokenAuth: []
          RoleHeaderAuth: []
          UserHeaderAuth: []
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "200":
          description: Shipping method list
    post:
      summary: Create or update shipping method
      security:
        - ApiTokenAuth: []
          RoleHeaderAuth: []
          UserHeaderAuth: []
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "201":
          description: Shipping method saved
  /identity/users:
    get:
      summary: List users
      security:
        - ApiTokenAuth: []
          UserHeaderAuth: []
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "200":
          description: Users list
    post:
      summary: Create or update user
      security:
        - ApiTokenAuth: []
          UserHeaderAuth: []
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "201":
          description: User saved
  /identity/auth/login:
    post:
      summary: Login and issue signed user JWT
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "200":
          description: Login success with token
  /identity/auth/refresh:
    post:
      summary: Rotate refresh token and issue a new signed user JWT (replay-safe)
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "200":
          description: Refresh success with rotated refresh token
  /identity/auth/logout:
    post:
      summary: Revoke active refresh token session (by refresh token hash)
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "200":
          description: Logout success
  /identity/auth/sessions:
    get:
      summary: List auth sessions for current user JWT subject
      security:
        - ApiTokenAuth: []
          UserHeaderAuth: []
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "200":
          description: Session list
  /identity/auth/sessions/{session_id}/revoke:
    post:
      summary: Revoke a specific auth session for current user JWT subject
      security:
        - ApiTokenAuth: []
          UserHeaderAuth: []
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
        - in: path
          name: session_id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Session revoked
  /identity/auth/sessions/revoke-others:
    post:
      summary: Revoke all other sessions except current refresh-token session
      security:
        - ApiTokenAuth: []
          UserHeaderAuth: []
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "200":
          description: Other sessions revoked
  /translations:
    post:
      summary: Upsert translation
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "201":
          description: Translation saved
  /metadata:
    post:
      summary: Upsert metadata entry
      security:
        - ApiTokenAuth: []
          RoleHeaderAuth: []
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "201":
          description: Metadata saved
  /search/index:
    post:
      summary: Index search document
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
        - $ref: "#/components/parameters/RegionHeader"
      responses:
        "201":
          description: Search document indexed
